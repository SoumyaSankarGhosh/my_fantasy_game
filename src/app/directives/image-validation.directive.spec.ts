import { ImageValidationDirective } from './image-validation.directive';

describe('ImageValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageValidationDirective();
    expect(directive).toBeTruthy();
  });
});
